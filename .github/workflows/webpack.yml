name: Webpack Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [17.x]

    steps:
    - uses: actions/checkout@v2

    - name: Node
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}

    - name: Build
      run: |
        npm install
        NODE_ENV=production npx webpack --mode production
        git config advice.addIgnoredFile false

    - name: Add and Commit
      uses: EndBug/add-and-commit@v7
      with:
        add: 'client/dist index.html CNAME --force'
        # author_name: Mikl Wolfe
        # author_email: wolfemikl@gmail.com
        branch: pages
        # committer_name: GitHub Actions
        # committer_email: actions@github.com
        cwd: '.'
        default_author: github_actions
        message: 'Update bundle'
        pathspec_error_handling: exitImmediately
        pull: 'NO-PULL'
        push: origin pages --set-upstream --force
        remove: '-r whitelist.json webpack.config.js server.properties README.md permissions.json package.json package-lock.json .gitignore .deepsource.toml worlds scripts dns client/src client/app.js .github'
        signoff: false
        # tag: 'v1.0.0 --force'
